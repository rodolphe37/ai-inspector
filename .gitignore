# OS / editor junk (shared)
.DS_Store
*.sw?
.idea
.vscode/*
!.vscode/extensions.json

# logs
*.log

# env files (each package keeps its own .env; never commit real values)
.env
.env.*
!.env.example
.claude